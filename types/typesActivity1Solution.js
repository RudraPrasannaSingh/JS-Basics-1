/*
 * Create a variable “stringYear” and set its value to string, “1995”
 * Create a variable “numberYear” and set its value to number, 1995
 * Use the + operator to “add” the two variables, print output
 * Find out the output data type using “typeof”
 */

let stringYear = "1995";
let numberYear = 1995;

let concatenated = stringYear + numberYear;
console.log(concatenated);
console.log(typeof concatenated);